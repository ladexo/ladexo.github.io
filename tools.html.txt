<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Security Tools — OloladeSec</title>
  <meta name="description" content="Interactive cybersecurity tools: password strength checker, password generator, and more.">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=JetBrains+Mono:wght@400;500;700&display=swap" rel="stylesheet">
  <style>
:root {
  --bg-primary: #0a0e17;
  --bg-secondary: #111827;
  --bg-card: rgba(17, 24, 39, 0.8);
  --bg-card-hover: rgba(17, 24, 39, 0.95);
  --accent-cyan: #00e5ff;
  --accent-green: #00ff88;
  --accent-purple: #a855f7;
  --accent-orange: #f97316;
  --accent-red: #ef4444;
  --accent-yellow: #facc15;
  --text-primary: #f1f5f9;
  --text-muted: #94a3b8;
  --border-subtle: rgba(148, 163, 184, 0.1);
  --border-glow: rgba(0, 229, 255, 0.2);
  --glass: rgba(15, 23, 42, 0.75);
  --radius: 12px;
  --radius-sm: 8px;
  --transition: 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  --font-body: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
  --font-mono: 'JetBrains Mono', 'Fira Code', monospace;
}
*, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }
html { scroll-behavior: smooth; }
body { font-family: var(--font-body); background: var(--bg-primary); color: var(--text-primary); line-height: 1.7; overflow-x: hidden; min-height: 100vh; }
a { color: var(--accent-cyan); text-decoration: none; transition: var(--transition); }
a:hover { color: var(--accent-green); }
.bg-grid { position: fixed; inset: 0; background-image: linear-gradient(rgba(0,229,255,0.03) 1px, transparent 1px), linear-gradient(90deg, rgba(0,229,255,0.03) 1px, transparent 1px); background-size: 60px 60px; pointer-events: none; z-index: 0; }
.scanline { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: repeating-linear-gradient(0deg, transparent, transparent 2px, rgba(0,229,255,0.008) 2px, rgba(0,229,255,0.008) 4px); pointer-events: none; z-index: 0; }
.container { max-width: 1100px; margin: 0 auto; padding: 0 1.5rem; }
.navbar { position: fixed; top: 0; left: 0; right: 0; z-index: 1000; background: var(--glass); backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px); border-bottom: 1px solid var(--border-subtle); transition: var(--transition); }
.navbar.scrolled { background: rgba(10, 14, 23, 0.95); box-shadow: 0 4px 30px rgba(0, 0, 0, 0.4); }
.nav-container { max-width: 1100px; margin: 0 auto; padding: 0 1.5rem; display: flex; align-items: center; justify-content: space-between; height: 70px; }
.nav-logo { display: flex; align-items: center; gap: 0.5rem; font-size: 1.3rem; font-weight: 800; letter-spacing: 2px; color: var(--text-primary); }
.logo-icon { font-size: 1.5rem; }
.nav-links { display: flex; list-style: none; gap: 2rem; }
.nav-links a { color: var(--text-muted); font-weight: 500; font-size: 0.95rem; padding: 0.5rem 0; position: relative; transition: var(--transition); }
.nav-links a::after { content: ''; position: absolute; bottom: 0; left: 0; width: 0; height: 2px; background: var(--accent-cyan); transition: var(--transition); }
.nav-links a:hover, .nav-links a.active { color: var(--accent-cyan); }
.nav-links a:hover::after, .nav-links a.active::after { width: 100%; }
.nav-toggle { display: none; flex-direction: column; gap: 5px; cursor: pointer; padding: 5px; }
.nav-toggle span { width: 24px; height: 2px; background: var(--text-primary); transition: var(--transition); border-radius: 2px; }
.nav-toggle.active span:nth-child(1) { transform: rotate(45deg) translate(5px, 5px); }
.nav-toggle.active span:nth-child(2) { opacity: 0; }
.nav-toggle.active span:nth-child(3) { transform: rotate(-45deg) translate(5px, -5px); }
.about-hero { position: relative; z-index: 1; text-align: center; padding: 8rem 1.5rem 3rem; }
.about-hero h1 { font-size: clamp(2rem, 4vw, 3rem); font-weight: 800; margin: 1.5rem 0 1rem; }
.about-hero p { color: var(--text-muted); font-size: 1.1rem; max-width: 600px; margin: 0 auto; }
.hero-badge { display: inline-flex; align-items: center; gap: 0.5rem; background: rgba(0, 255, 136, 0.08); border: 1px solid rgba(0, 255, 136, 0.25); padding: 0.5rem 1.2rem; border-radius: 50px; font-size: 0.85rem; font-weight: 500; color: var(--accent-green); margin-bottom: 2rem; font-family: var(--font-mono); }
.pulse-dot { width: 8px; height: 8px; background: var(--accent-green); border-radius: 50%; animation: pulse 2s infinite; }
@keyframes pulse { 0%, 100% { opacity: 1; box-shadow: 0 0 0 0 rgba(0, 255, 136, 0.5); } 50% { opacity: 0.7; box-shadow: 0 0 0 8px rgba(0, 255, 136, 0); } }
.gradient-text { background: linear-gradient(135deg, var(--accent-cyan), var(--accent-green)); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; }
.hero-buttons { display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; }
.btn { display: inline-flex; align-items: center; gap: 0.5rem; padding: 0.85rem 2rem; border-radius: var(--radius-sm); font-weight: 600; font-size: 0.95rem; border: none; cursor: pointer; transition: var(--transition); font-family: var(--font-body); text-decoration: none; }
.btn-primary { background: linear-gradient(135deg, var(--accent-cyan), #00b4d8); color: var(--bg-primary); box-shadow: 0 0 20px rgba(0, 229, 255, 0.2); }
.btn-primary:hover { box-shadow: 0 0 35px rgba(0, 229, 255, 0.4); transform: translateY(-2px); color: var(--bg-primary); }
.btn-outline { background: transparent; color: var(--text-primary); border: 1px solid var(--border-glow); }
.btn-outline:hover { border-color: var(--accent-cyan); background: rgba(0, 229, 255, 0.05); color: var(--accent-cyan); }
.section { position: relative; z-index: 1; padding: 5rem 1.5rem; }
.tools-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 1.5rem; margin-bottom: 2rem; }
.tool-preview-card { background: var(--bg-card); border: 1px solid var(--border-subtle); border-radius: var(--radius); padding: 1.8rem; position: relative; transition: var(--transition); }
.tool-preview-card:hover { border-color: var(--border-glow); transform: translateY(-3px); }
.tool-preview-card h3 { font-size: 1.05rem; margin-bottom: 0.6rem; }
.tool-preview-card p { color: var(--text-muted); font-size: 0.88rem; margin-bottom: 1.2rem; line-height: 1.6; }
.tool-status { display: inline-block; font-size: 0.72rem; font-weight: 600; padding: 0.25rem 0.75rem; border-radius: 50px; margin-bottom: 0.8rem; font-family: var(--font-mono); text-transform: uppercase; letter-spacing: 1px; }
.tool-status.live { background: rgba(0, 255, 136, 0.1); color: var(--accent-green); border: 1px solid rgba(0, 255, 136, 0.2); }
.tool-status.coming { background: rgba(250, 204, 21, 0.08); color: var(--accent-yellow); border: 1px solid rgba(250, 204, 21, 0.2); }
.tool-container { max-width: 800px; margin: 0 auto; }
.tool-card { background: var(--bg-card); border: 1px solid var(--border-glow); border-radius: var(--radius); padding: 2.5rem; }
.tool-card h3 { font-size: 1.4rem; margin-bottom: 0.5rem; }
.tool-desc { color: var(--text-muted); font-size: 0.92rem; margin-bottom: 2rem; }
.password-input-wrap { position: relative; margin-bottom: 1.5rem; }
.password-input-wrap input { width: 100%; padding: 1rem 3.5rem 1rem 1.2rem; background: rgba(0, 0, 0, 0.4); border: 1px solid var(--border-subtle); border-radius: var(--radius-sm); color: var(--text-primary); font-family: var(--font-mono); font-size: 1rem; outline: none; transition: var(--transition); }
.password-input-wrap input:focus { border-color: var(--accent-cyan); box-shadow: 0 0 15px rgba(0, 229, 255, 0.1); }
.toggle-visibility { position: absolute; right: 1rem; top: 50%; transform: translateY(-50%); background: none; border: none; font-size: 1.2rem; cursor: pointer; color: var(--text-muted); padding: 0.2rem; transition: var(--transition); }
.toggle-visibility:hover { color: var(--accent-cyan); }
.strength-meter { margin-bottom: 1.5rem; }
.meter-bar-bg { width: 100%; height: 6px; background: rgba(255, 255, 255, 0.05); border-radius: 3px; overflow: hidden; margin-bottom: 0.5rem; }
.meter-bar-fill { height: 100%; width: 0%; border-radius: 3px; transition: width 0.5s ease, background 0.5s ease; }
.meter-label { display: flex; justify-content: space-between; font-size: 0.85rem; }
.strength-text { font-weight: 600; }
.crack-time { color: var(--text-muted); font-family: var(--font-mono); font-size: 0.8rem; }
.criteria-list { list-style: none; display: grid; grid-template-columns: repeat(2, 1fr); gap: 0.5rem; margin-bottom: 2rem; padding: 1.2rem; background: rgba(0, 0, 0, 0.2); border-radius: var(--radius-sm); border: 1px solid var(--border-subtle); }
.criteria-list li { display: flex; align-items: center; gap: 0.5rem; font-size: 0.85rem; color: var(--text-muted); transition: var(--transition); }
.criteria-list li.met { color: var(--accent-green); }
.check-icon { width: 20px; height: 20px; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 0.7rem; background: rgba(255, 255, 255, 0.05); color: var(--text-muted); transition: var(--transition); flex-shrink: 0; }
.criteria-list li.met .check-icon { background: rgba(0, 255, 136, 0.15); color: var(--accent-green); }
.generator-section { border-top: 1px solid var(--border-subtle); padding-top: 2rem; margin-top: 0.5rem; }
.generator-section h4 { font-size: 1.1rem; margin-bottom: 1.5rem; }
.length-slider { display: flex; align-items: center; gap: 1rem; margin-bottom: 1.2rem; }
.length-slider label { font-size: 0.9rem; color: var(--text-muted); min-width: 55px; }
.length-slider input[type="range"] { flex: 1; -webkit-appearance: none; appearance: none; height: 4px; background: rgba(255, 255, 255, 0.1); border-radius: 2px; outline: none; }
.length-slider input[type="range"]::-webkit-slider-thumb { -webkit-appearance: none; width: 18px; height: 18px; background: var(--accent-cyan); border-radius: 50%; cursor: pointer; box-shadow: 0 0 8px rgba(0, 229, 255, 0.4); }
.length-value { font-family: var(--font-mono); font-weight: 700; color: var(--accent-cyan); min-width: 28px; text-align: right; }
.gen-options { display: flex; flex-wrap: wrap; gap: 1rem; margin-bottom: 1.5rem; }
.gen-options label { display: flex; align-items: center; gap: 0.4rem; font-size: 0.88rem; color: var(--text-muted); cursor: pointer; }
.gen-options input[type="checkbox"] { accent-color: var(--accent-cyan); width: 16px; height: 16px; }
.gen-output { display: flex; gap: 0.5rem; }
.gen-output input { flex: 1; padding: 0.8rem 1rem; background: rgba(0, 0, 0, 0.4); border: 1px solid var(--border-subtle); border-radius: var(--radius-sm); color: var(--accent-green); font-family: var(--font-mono); font-size: 0.9rem; outline: none; }
.btn-generate, .btn-copy { padding: 0.8rem 1.2rem; border: 1px solid var(--border-glow); background: rgba(0, 229, 255, 0.08); color: var(--accent-cyan); font-weight: 600; font-size: 0.85rem; border-radius: var(--radius-sm); cursor: pointer; transition: var(--transition); font-family: var(--font-body); white-space: nowrap; }
.btn-generate:hover, .btn-copy:hover { background: rgba(0, 229, 255, 0.15); border-color: var(--accent-cyan); }
.about-block { background: var(--bg-card); border: 1px solid var(--border-subtle); border-radius: var(--radius); padding: 2rem; margin-bottom: 1.5rem; }
.about-block h2 { font-size: 1.3rem; font-weight: 700; margin-bottom: 1rem; }
.about-block ul { list-style: none; padding: 0; }
.about-block ul li { color: var(--text-muted); padding: 0.4rem 0 0.4rem 1.5rem; position: relative; line-height: 1.7; }
.about-block ul li::before { content: '›'; position: absolute; left: 0; color: var(--accent-cyan); font-weight: 700; }
.footer { position: relative; z-index: 1; border-top: 1px solid var(--border-subtle); padding: 3rem 1.5rem; text-align: center; }
.footer-content { max-width: 1100px; margin: 0 auto; }
.footer-logo { font-size: 1.2rem; font-weight: 800; letter-spacing: 2px; margin-bottom: 1rem; }
.footer-links { display: flex; justify-content: center; gap: 2rem; margin-bottom: 1.5rem; }
.footer-links a { color: var(--text-muted); font-size: 0.9rem; }
.footer-links a:hover { color: var(--accent-cyan); }
.footer-copy { color: var(--text-muted); font-size: 0.8rem; opacity: 0.7; }
.fade-in { opacity: 0; transform: translateY(25px); transition: opacity 0.6s ease, transform 0.6s ease; }
.fade-in.visible { opacity: 1; transform: translateY(0); }
@media (max-width: 768px) {
  .nav-links { display: none; position: absolute; top: 70px; left: 0; right: 0; background: rgba(10, 14, 23, 0.98); backdrop-filter: blur(20px); flex-direction: column; align-items: center; padding: 2rem 0; gap: 1.5rem; border-bottom: 1px solid var(--border-subtle); }
  .nav-links.open { display: flex; }
  .nav-toggle { display: flex; }
  .tools-grid { grid-template-columns: 1fr; }
  .criteria-list { grid-template-columns: 1fr; }
  .gen-output { flex-direction: column; }
  .tool-card { padding: 1.5rem; }
  .about-hero { padding-top: 6rem; }
}
  </style>
</head>
<body>
  <div class="bg-grid"></div>
  <div class="scanline"></div>
  <nav class="navbar">
    <div class="nav-container">
      <a href="index.html" class="nav-logo">
        <span class="logo-icon">🛡️</span>
        <span>OLOLADE<span style="color: var(--accent-green);">SEC</span></span>
      </a>
      <ul class="nav-links">
        <li><a href="index.html">Home</a></li>
        <li><a href="tools.html" class="active">Tools</a></li>
        <li><a href="about.html">About</a></li>
      </ul>
      <div class="nav-toggle"><span></span><span></span><span></span></div>
    </div>
  </nav>
  <section class="about-hero">
    <div class="hero-badge"><span class="pulse-dot"></span> Security Toolkit</div>
    <h1>Interactive <span class="gradient-text">Security Tools</span></h1>
    <p>Free, client-side tools that run entirely in your browser. No data is ever sent to a server — your information stays private.</p>
  </section>
  <section class="section" style="padding-top: 0;">
    <div class="container">
      <div class="tools-grid fade-in">
        <div class="tool-preview-card">
          <span class="tool-status live">● Live</span>
          <h3>🔐 Password Strength Checker</h3>
          <p>Analyze any password's strength with real-time scoring, crack-time estimation, and detailed security criteria feedback.</p>
          <a href="#password-checker" class="btn btn-primary" style="font-size:0.85rem;padding:0.6rem 1.5rem;">Use Tool ↓</a>
        </div>
        <div class="tool-preview-card">
          <span class="tool-status live">● Live</span>
          <h3>⚡ Password Generator</h3>
          <p>Generate cryptographically secure passwords with customizable length, character sets, and complexity requirements.</p>
          <a href="#password-checker" class="btn btn-primary" style="font-size:0.85rem;padding:0.6rem 1.5rem;">Use Tool ↓</a>
        </div>
        <div class="tool-preview-card">
          <span class="tool-status coming">◐ Coming Soon</span>
          <h3>🌐 Header Analyzer</h3>
          <p>Inspect HTTP security headers of any website to identify misconfigurations and missing protections like CSP, HSTS, and X-Frame.</p>
          <span class="btn btn-outline" style="font-size:0.85rem;padding:0.6rem 1.5rem;opacity:0.5;cursor:default;">In Development</span>
        </div>
        <div class="tool-preview-card">
          <span class="tool-status coming">◐ Coming Soon</span>
          <h3>📧 Email Breach Check</h3>
          <p>Verify whether an email address has appeared in known data breaches and get actionable remediation steps.</p>
          <span class="btn btn-outline" style="font-size:0.85rem;padding:0.6rem 1.5rem;opacity:0.5;cursor:default;">In Development</span>
        </div>
      </div>
    </div>
  </section>
  <section class="section" id="password-checker">
    <div class="tool-container">
      <div class="tool-card fade-in">
        <h3>🔐 Password Strength Checker</h3>
        <p class="tool-desc">Enter a password below to analyze its strength. Everything runs locally in your browser — nothing leaves your device.</p>
        <div class="password-input-wrap">
          <input type="password" id="passwordInput" placeholder="Type a password to analyze..." autocomplete="off" spellcheck="false">
          <button class="toggle-visibility" title="Toggle visibility">👁️</button>
        </div>
        <div class="strength-meter">
          <div class="meter-bar-bg"><div class="meter-bar-fill"></div></div>
          <div class="meter-label">
            <span class="strength-text" style="color:var(--text-muted);">—</span>
            <span class="crack-time"></span>
          </div>
        </div>
        <ul class="criteria-list">
          <li id="cr-length"><span class="check-icon">✓</span> 12+ characters</li>
          <li id="cr-upper"><span class="check-icon">✓</span> Uppercase letter</li>
          <li id="cr-lower"><span class="check-icon">✓</span> Lowercase letter</li>
          <li id="cr-number"><span class="check-icon">✓</span> Contains number</li>
          <li id="cr-special"><span class="check-icon">✓</span> Special character</li>
          <li id="cr-nocommon"><span class="check-icon">✓</span> Not a common password</li>
        </ul>
        <div class="generator-section">
          <h4>⚡ Generate Secure Password</h4>
          <div class="length-slider">
            <label>Length:</label>
            <input type="range" id="genLength" min="8" max="64" value="20">
            <span class="length-value">20</span>
          </div>
          <div class="gen-options">
            <label><input type="checkbox" id="genUpper" checked> Uppercase</label>
            <label><input type="checkbox" id="genLower" checked> Lowercase</label>
            <label><input type="checkbox" id="genNumbers" checked> Numbers</label>
            <label><input type="checkbox" id="genSymbols" checked> Symbols</label>
          </div>
          <div class="gen-output">
            <input type="text" id="genOutput" readonly placeholder="Click Generate...">
            <button class="btn-generate">Generate</button>
            <button class="btn-copy">📋 Copy</button>
          </div>
        </div>
      </div>
      <div class="about-block fade-in" style="margin-top:2rem;">
        <h2>💡 Password Security Tips</h2>
        <ul>
          <li>Use a unique password for every account — never reuse passwords across services</li>
          <li>Aim for at least 16 characters to resist modern brute-force attacks</li>
          <li>Use a reputable password manager to store and autofill complex passwords</li>
          <li>Enable multi-factor authentication (MFA) wherever available</li>
          <li>Passphrases (e.g., "correct-horse-battery-staple") are both strong and memorable</li>
          <li>Change passwords immediately if a service you use reports a data breach</li>
          <li>Avoid personal information like birthdays, pet names, or common substitutions (@ for a)</li>
        </ul>
      </div>
    </div>
  </section>
  <footer class="footer">
    <div class="footer-content">
      <div class="footer-logo">OLOLADE<span style="color:var(--accent-green);">SEC</span></div>
      <div class="footer-links">
        <a href="index.html">Home</a>
        <a href="tools.html">Tools</a>
        <a href="about.html">About</a>
      </div>
      <p class="footer-copy">&copy; 2026 OloladeSec. Built with precision. All rights reserved.</p>
    </div>
  </footer>
  <script>
const navToggle=document.querySelector('.nav-toggle'),navLinks=document.querySelector('.nav-links');
if(navToggle){navToggle.addEventListener('click',()=>{navToggle.classList.toggle('active');navLinks.classList.toggle('open')});document.querySelectorAll('.nav-links a').forEach(l=>l.addEventListener('click',()=>{navToggle.classList.remove('active');navLinks.classList.remove('open')}))}
const navbar=document.querySelector('.navbar');window.addEventListener('scroll',()=>{window.scrollY>50?navbar.classList.add('scrolled'):navbar.classList.remove('scrolled')});
const fadeEls=document.querySelectorAll('.fade-in');const fadeObs=new IntersectionObserver(e=>{e.forEach(en=>{if(en.isIntersecting)en.target.classList.add('visible')})},{threshold:0.1,rootMargin:'0px 0px -40px 0px'});fadeEls.forEach(el=>fadeObs.observe(el));
const passwordInput=document.getElementById('passwordInput'),toggleBtn=document.querySelector('.toggle-visibility');
if(passwordInput){
if(toggleBtn){toggleBtn.addEventListener('click',()=>{const t=passwordInput.type==='password'?'text':'password';passwordInput.type=t;toggleBtn.textContent=t==='password'?'👁️':'🔒'})}
const commonPw=new Set(['password','123456','12345678','qwerty','abc123','monkey','master','dragon','111111','baseball','iloveyou','trustno1','sunshine','ashley','football','shadow','123123','654321','superman','qazwsx','michael','password1','password123','admin','letmein','welcome','login','starwars','1234567','12345','1234567890','000000','passw0rd','hello','charlie']);
passwordInput.addEventListener('input',()=>{
const pw=passwordInput.value,fill=document.querySelector('.meter-bar-fill'),st=document.querySelector('.strength-text'),ct=document.querySelector('.crack-time');
const cr={length:pw.length>=12,upper:/[A-Z]/.test(pw),lower:/[a-z]/.test(pw),number:/\d/.test(pw),special:/[^A-Za-z0-9]/.test(pw),nocommon:!commonPw.has(pw.toLowerCase())};
Object.keys(cr).forEach(k=>{const el=document.getElementById('cr-'+k);if(el){cr[k]?el.classList.add('met'):el.classList.remove('met')}});
if(pw.length===0){fill.style.width='0%';fill.style.background='transparent';st.textContent='—';st.style.color='var(--text-muted)';ct.textContent='';return}
let sc=0;Object.values(cr).forEach(v=>{if(v)sc++});if(pw.length>=16)sc++;if(pw.length>=20)sc++;
let cs=0;if(/[a-z]/.test(pw))cs+=26;if(/[A-Z]/.test(pw))cs+=26;if(/\d/.test(pw))cs+=10;if(/[^A-Za-z0-9]/.test(pw))cs+=33;if(cs===0)cs=26;
const comb=Math.pow(cs,pw.length),gpu=10e9,sec=comb/gpu;
let ts='';if(sec<0.001)ts='Instantly';else if(sec<1)ts='Under 1 second';else if(sec<60)ts=Math.ceil(sec)+' seconds';else if(sec<3600)ts=Math.ceil(sec/60)+' minutes';else if(sec<86400)ts=Math.ceil(sec/3600)+' hours';else if(sec<31536000)ts=Math.ceil(sec/86400)+' days';else if(sec<31536000*1000)ts=Math.ceil(sec/31536000)+' years';else if(sec<31536000*1e6)ts=Math.ceil(sec/31536000/1000)+'K years';else if(sec<31536000*1e9)ts=Math.ceil(sec/31536000/1e6)+'M years';else ts='∞ (centuries+)';
let lv,co,w;if(sc<=2){lv='Weak';co='#ef4444';w='20%'}else if(sc<=3){lv='Fair';co='#f97316';w='40%'}else if(sc<=5){lv='Good';co='#facc15';w='60%'}else if(sc<=6){lv='Strong';co='#00ff88';w='80%'}else{lv='Excellent';co='#00e5ff';w='100%'}
if(!cr.nocommon){lv='Compromised';co='#ef4444';w='10%';ts='Instantly (in breach databases)'}
fill.style.width=w;fill.style.background=co;st.textContent=lv;st.style.color=co;ct.textContent='Est. crack time (GPU): '+ts})}
const genLength=document.getElementById('genLength'),genOutput=document.getElementById('genOutput'),genBtn=document.querySelector('.btn-generate'),copyBtn=document.querySelector('.btn-copy'),lengthVal=document.querySelector('.length-value');
if(genLength&&lengthVal){genLength.addEventListener('input',()=>{lengthVal.textContent=genLength.value})}
if(genBtn){genBtn.addEventListener('click',()=>{const len=parseInt(genLength.value);let ch='';if(document.getElementById('genUpper').checked)ch+='ABCDEFGHIJKLMNOPQRSTUVWXYZ';if(document.getElementById('genLower').checked)ch+='abcdefghijklmnopqrstuvwxyz';if(document.getElementById('genNumbers').checked)ch+='0123456789';if(document.getElementById('genSymbols').checked)ch+='!@#$%^&*()_+-=[]{}|;:,.<>?';if(ch.length===0){genOutput.value='Select at least one character type';return}const arr=new Uint32Array(len);crypto.getRandomValues(arr);let pw='';for(let i=0;i<len;i++)pw+=ch[arr[i]%ch.length];genOutput.value=pw;genOutput.style.borderColor='var(--accent-green)';setTimeout(()=>{genOutput.style.borderColor='var(--border-subtle)'},800)})}
if(copyBtn){copyBtn.addEventListener('click',()=>{if(genOutput&&genOutput.value&&genOutput.value!=='Click Generate...'&&genOutput.value!=='Select at least one character type'){navigator.clipboard.writeText(genOutput.value).then(()=>{const o=copyBtn.textContent;copyBtn.textContent='✓ Copied';copyBtn.style.color='var(--accent-green)';copyBtn.style.borderColor='var(--accent-green)';setTimeout(()=>{copyBtn.textContent=o;copyBtn.style.color='';copyBtn.style.borderColor=''},2000)})}})}
  </script>
</body>
</html>
